// FUNCION DECLARATIVA

function saludar(nombre) {
    console.log(`Hola ${nombre} hoy es un nuevo día`);
}

saludar("Pao");


function saludar(name) {
    console.log(`Hola coder ${name}`);
}

saludar("Shirly");

function despedir(name) {
    console.log("Hasta pronto coder ", name);
}

despedir("Shirly");

// usuario y contraseña, si el usuario "admin" y la contraseña es 12345 debo mostrar el mesaje "bienvenido NombreDelUsuario"
// En caso de que el login no sea correto debe mostrar por consola y por alerta una mesaje de error

function login(user, pasword) {
    if (user == "adimin" && pasword === 1234) {
        console.log(" ");
    }

    else{

    }
}